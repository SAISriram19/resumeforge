# Root level environment variables (not currently used)
# This project uses separate .env files:
# - client/.env (for frontend Vite app)
# - server/.env (for backend Express app)
# 
# See client/.env.example and server/.env.example for the required variables
